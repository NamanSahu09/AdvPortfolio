version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Installing Node.js 20..."
        - nvm install 20
        - nvm use 20
        - node -v
        - npm ci
    build:
      commands:
        - echo "🚀 Building project..."
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .npm/**
      - .vite/**
